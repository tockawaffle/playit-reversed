In order for the tests to run, you will have to first run `bun run playit:setup` to create the dynamic typings and the necessary data for it to be used.

You will also have to change some things as the tests were made by using my own generated data.